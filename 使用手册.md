# 隐私政策管理台 · 使用手册

> 版本 V1.1 · 单文件静态应用 · 支持隐私政策 / 用户协议 / 会员服务协议三类协议文本的统一管理

---

## 1. 系统简介

「隐私政策管理台」是一款**纯前端单文件应用**（`index.html`），面向 App 合规运营场景，用于集中管理各端应用的**隐私政策、用户协议、会员服务协议**三类协议文本：

- **应用台账**：登记 App 名称、Android / iOS / HarmonyOS 包名、当前版本号；
- **链接检测**：定时或手动检测三类协议在线链接是否可达，失效链接红色预警；
- **快照存档**：手动上传 HTML 或在线抓取时自动存档，每类协议独立版本链（V001、V002…）；
- **版本对比**：任意两份快照并排预览 + 逐行文本差异；
- **防篡改**：每份快照计算 SHA-256 内容指纹，并按协议维度串联哈希链，可一键校验完整性。

### 1.1 部署形态与数据位置（重要）

| 项目 | 说明 |
| --- | --- |
| 程序部署位置 | `index.html` 部署在**静态托管服务**上（当前为 GitHub Pages），也可放在任意 Web 服务器 / 对象存储 / 内网文件服务器，无需后端 |
| 数据存储位置 | 全部业务数据（应用记录、快照、配置、动态日志）保存在**访问者浏览器的 localStorage** 中，不上传任何服务器 |
| 敏感凭据 | 登录密码经 PBKDF2 派生哈希后存储；验证器密钥经认证加密封装存储，均只存在于当前浏览器 |

> ⚠️ 换浏览器或换电脑不会带过去数据。需要迁移时请在「设置 → 数据」中导出 JSON 备份，再到新环境导入。

---

## 2. 访问与首次初始化

1. 浏览器打开部署地址（如 GitHub Pages 域名）；
2. 首次访问进入**初始化向导**，共两步：
   - **① 设置密码**：设置管理密码（至少 8 位），用于每次登录；
   - **② 绑定验证器**：打开手机 **Microsoft Authenticator** → 添加账户 → **其他账户** → 扫描页面二维码（无法扫码时，手动输入页面显示的 Secret 密钥，6 位数字、30 秒刷新），然后输入验证器显示的 6 位验证码完成绑定。
3. 初始化完成后自动进入管理台。

> 建议正式部署通过 HTTPS 访问（GitHub Pages 默认即为 HTTPS）。HTTP 环境下系统会自动启用内置加密后备实现，功能不受影响。

---

## 3. 登录与锁定

- **登录**：输入管理密码解锁；连续输错 5 次将锁定 60 秒。
- **锁定**：点击左侧边栏底部「锁定管理台」立即退出登录态（验证器密钥仅登录期间驻留内存）。
- **敏感操作二次验证**：上传、下载、删除、导出、导入等操作会弹出 TOTP 验证码确认框（30 秒刷新），需输入验证器上的 6 位码；连续输错 5 次同样锁定 60 秒。

---

## 4. 应用管理

### 4.1 新建应用

「应用管理」→「新建应用」，填写：

| 字段 | 说明 |
| --- | --- |
| 应用名称 * | 如「示例出行」 |
| Android 包名 | 如 `com.example.app` |
| iOS Bundle ID | 如 `com.example.app` |
| HarmonyOS 包名 | 如 `com.example.app` |
| 当前版本号 | 如 `1.0.0` |
| 隐私政策在线链接 * | 必填，`https://example.com/privacy.html` |
| 用户协议在线链接 | 选填，可稍后编辑补充 |
| 会员服务协议在线链接 | 选填，可稍后编辑补充 |

### 4.2 应用列表

列表页展示每个应用的包名芯片、版本、**三类协议的在线链接**（带「隐私 / 用户 / 会员」标签）、链接状态芯片、快照总数。

- **链接状态芯片**：每个协议一枚，绿色 = 正常（显示 HTTP 状态码或「可达」），红色 = 异常，灰色 = 未检测；
- **失效预警**：任一协议链接异常时整行红色高亮，左侧导航「应用管理」出现红色角标；
- **行操作**：刷新（检测连通）｜上传 HTML｜编辑｜删除（删除应用会连带删除其全部快照，需二次确认）。
- 点击应用行进入**应用详情页**。

### 4.3 应用详情页（协议 Tab）

详情页顶部为应用基本信息与操作按钮，下方是**三类协议的 Tab**（隐私政策 / 用户协议 / 会员服务协议，Tab 上显示各自快照数量），每个 Tab 内是**该协议独立**的快照版本列表。

顶部操作：

| 按钮 | 说明 |
| --- | --- |
| 检测连通 | 并行检测该应用已登记的所有协议链接 |
| 上传 隐私政策 / 用户协议 / 会员服务协议 | 上传该 Tab 对应协议的 HTML 文件，生成快照 |
| 下载当前生效 | 下载该 Tab 对应协议最新一份快照（V 编号最大者） |
| 导出历史快照 | 将该应用**全部协议**的快照打包导出为单个离线 HTML 档案 |

---

## 5. 链接检测

### 5.1 触发方式

- **手动**：应用行的刷新按钮（单应用）或「一键检测全部」；
- **自动**：打开平台时，若距上次全量检测超过设定间隔（默认 30 分钟，可在设置中调整）自动触发。

### 5.2 检测通道

检测按以下顺序尝试，保证结果尽量准确：

1. **CORS 直连**：浏览器直接请求目标链接，可读到 HTTP 状态码与内容；
2. **公共代理**：直连失败（如跨域限制）时经代理抓取；
3. **跨域兜底**（`no-cors`）：只能确认「可达 / 不可达」，芯片显示「可达」而非状态码。

### 5.3 自动快照

检测过程中若能取到在线 HTML 内容，系统会将其与该协议最新快照比对：**内容指纹不一致时自动生成新快照**（来源标记为「在线抓取」），实现线上内容变更的自动留痕。

---

## 6. 快照管理

### 6.1 生成快照的两种方式

| 方式 | 说明 |
| --- | --- |
| 本地上传 | 点击「上传 ××」，选择 `.html` / `.htm` 文件，经二次验证后存档 |
| 在线抓取 | 链接检测时发现线上内容与最新快照不一致，自动存档 |

每类协议的版本号（V001、V002…）与哈希链**相互独立**。

### 6.2 上传 ≠ 部署（重要）

**上传 HTML 只是在本系统内生成快照存档，不会修改服务器上的网页。** 本系统是纯前端工具，没有任何后端写入能力。上传成功弹窗会给出建议的**发布路径**（格式：`域名/应用标识/协议文件名`，如 `…/privacy.html`、`…/user-agreement.html`、`…/membership.html`）：

- 若勾选「同时将该应用的××在线链接更新为上述发布路径」，则系统会用该路径作为检测目标；
- **该路径只是建议位置**。文件未真正部署到服务器前，直接访问该路径会返回 404，**属正常现象**；将快照文件下载并部署到该位置后即可正常访问。

### 6.3 快照操作

| 操作 | 说明 |
| --- | --- |
| 预览 | 弹窗内直接渲染快照内容（禁用脚本运行），与网络路径无关，不会因未部署而 404 |
| 下载 | 下载该快照的 HTML 文件（文件名含协议类型与版本号） |
| 删除 | 二次确认后删除；该协议的哈希链会自动重建，历史版本出现空缺 |
| 对比 | 勾选任意两份快照 → 底部对比栏 →「对比所选版本」，支持并排预览与逐行文本差异（含增删行统计） |

---

## 7. 快照库

「快照库」汇总所有应用的全部快照，支持三个维度筛选：**应用 / 协议（隐私政策、用户协议、会员服务协议）/ 来源（本地上传、在线抓取）**。每条记录显示协议标签、版本、抓取时间、来源、大小与内容指纹（点击复制完整指纹）。

---

## 8. 设置

| 设置项 | 说明 |
| --- | --- |
| 文件存放域名 | 协议 HTML 的部署根地址（如 `https://privacy.example.com`），用于生成建议发布路径 |
| 链接检测频率 | 5 分钟 ~ 24 小时；超过间隔打开平台自动检测 |
| 修改密码 | 需验证当前密码 + TOTP 验证码 |
| 重新绑定验证器 | 验证密码后生成新二维码 / Secret，重新扫码绑定 |
| 快照完整性校验 | 按「应用 × 协议」逐链校验内容指纹与链式哈希 |
| 备份导出 / 导入 | JSON 格式，含应用、快照、设置、动态；导入可选「合并」或「覆盖」，经二次验证 |
| 清空全部数据 | 删除所有数据并重置到初始化状态（需重新设置密码与验证器） |

侧边栏底部实时显示**本地存储用量**（上限约 5 MB）。快照过多接近上限时，请删除部分历史快照或导出备份后清理。

---

## 9. 安全机制一览

| 机制 | 实现 |
| --- | --- |
| 登录密码 | PBKDF2（SHA-256，150,000 轮）派生哈希校验，绝不明文存储 |
| 二次验证 | TOTP（RFC 6238，SHA-1，6 位，30 秒），Microsoft Authenticator 等均兼容 |
| 快照防篡改 | 每份快照含 SHA-256 内容指纹；同应用同协议的快照按时间顺序串联哈希链，任一内容或顺序被改动，其后所有版本校验失败 |
| 会话安全 | 验证器密钥仅登录期间驻留内存，锁定即清除 |
| 传输安全 | 建议通过 HTTPS 部署访问 |

---

## 10. 常见问题（FAQ）

**Q1：上传 HTML 能更新服务器上的网页吗？**
不能。上传仅生成快照存档。需要线上生效时：下载快照文件 → 自行部署到你的服务器（建议按发布路径 `域名/应用标识/协议文件名` 存放）→ 部署后可再次检测确认线上内容与快照一致。

**Q2：系统部署在服务器还是本地？**
程序（`index.html`）部署在静态托管服务器上（如 GitHub Pages），用户用浏览器访问；数据全部存在**访问者浏览器本地**（localStorage），服务器上不保存任何业务数据。

**Q3：手动上传后，访问发布路径显示 404 正常吗？**
正常。发布路径只是系统按「域名/应用标识/协议文件名」生成的**建议位置**，文件尚未部署前该路径自然 404；部署后即可访问。而**快照预览**是直接渲染存档内容，不受此影响、不会 404。

**Q4：换了浏览器 / 电脑，数据怎么迁移？**
旧环境「设置 → 数据 → 备份导出」得到 JSON → 新环境「备份导入」（可选合并或覆盖）。注意：备份不含密码与验证器密钥，新环境首次使用仍需完成初始化。

**Q5：检测显示「可达」而不是 HTTP 200？**
目标站点未开放跨域读取，系统只能以跨域兜底方式确认链接可达，无法读取状态码。若需精确状态码，可在目标站点为托管域名配置 CORS 响应头。

**Q6：忘记管理密码怎么办？**
密码无法找回（系统不留存明文）。可清空浏览器对应站点的 localStorage 后重新初始化，但原有数据将丢失——请务必定期导出备份，并妥善保管密码。

---

## 11. 版本记录

- **V1.1**：应用管理扩展为三协议（隐私政策 / 用户协议 / 会员服务协议）；每协议独立链接检测、独立快照版本链与哈希链；快照库新增协议筛选；上传流程支持协议选择；快照预览修复相对路径导致的 404 问题；三协议并行检测提速。
- **V1.0**：首个版本：应用台账、隐私政策链接检测、快照存档与版本对比、TOTP 二次验证、备份导入导出。
